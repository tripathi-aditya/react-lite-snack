export enum CONTAINER_POSITIONS {
  TOP = "top",
  RIGHT = "right",
  BOTTOM = "bottom",
  LEFT = "left",
}
export type LOADING_DIRECTIONS = "top-down" | "bottom-up";
export type VARIANTS = "success" | "failure" | "warning";
